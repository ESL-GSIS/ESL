@model IEnumerable<ESL.Models.SchoolStudent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.School.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.School1.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.School2.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.School3.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.LastName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.School.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.School1.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.School2.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.School3.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.LastName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
